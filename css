<style>
CSS

.link {
    stroke: #999;
    stroke-opacity: 0.6;
}

.node {
    fill: #ccc;
    stroke: #fff;
    stroke-width: 1.5px;
}

@keyframes pulse {
    0% { r: 5; }
    50% { r: 10; }
    100% { r: 5; }
}

@keyframes flash {
    0% { stroke-opacity: 0.1; }
    50% { stroke-opacity: 1; }
    100% { stroke-opacity: 0.1; }
}

.node {
    animation: pulse 2s infinite;
}

.link {
    animation: flash 1s infinite;
}
