export const CONFIG = Object.freeze({
  particleCount: 100,
  maxVelocity: 0.5,
  connectionDistance: 120,
  dotColor: '#0f0',
  lineBaseAlpha: 0.2,
  pullStrength: 0.0005,
  gradientRadius: 120,
  sinkRadius: 160 // only particles within this radius are affected
});
